---
import Layout from "../layouts/Layout.astro";
import "../styles/global.css";
import { pathResolver } from "../utils/path-resolver";

const pathName = pathResolver(Astro.originPathname);
---

<Layout>
    <h1>{pathName}</h1>
</Layout>
