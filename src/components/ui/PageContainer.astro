---
interface Props {
    class?: string;
}

const { class: className } = Astro.props as Props;

const baseClass = "mx-auto w-full max-w-6xl px-6 sm:px-8 lg:px-10";
const classes = [baseClass, className].filter(Boolean).join(" ");
---

<div class={classes}>
    <slot />
</div>
