export const languages = {
    en: "English",
    es: "Spanish",
};

export const defaultLang = "en";

export const ui = {
    en: {},
    es: {},
} as const;

export const languagesArray = Object.keys(languages);
